void main() {
  int x = (10 + 3) % 4;
  print(x);
  // up until now we have been talking about expression and statement.
  //expressions hold a value at runtime.
  // (10+3), (x +=10), [age > 16? 'adult' : 'child'] these have value, when assigned to a variable 
  //Otherhand statement do not hold a value.
  // print('next: ${value + 1}')
  // int y  = x * 2;
  // print('hello');
  // have no more value further to assigned or modified.
}